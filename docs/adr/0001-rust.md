# 0001. Rust for the binary

Status: accepted

A soak runs for hours: sample, play, rewrite the pack. The ship is one Rust binary so overnight reliability and a single artifact come first. Family adapters stay small parsers (RFCOMM or AAP) so they can be reviewed without the rest of the stack.
